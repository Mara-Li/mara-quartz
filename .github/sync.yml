group:
  - files:
      - quartz/
      - source: .github/workflows/
        dest: .github/workflows/
        exclude: |
          sync_file.yml
          ci.yaml
    repos: |
      ObsidianPublisher/Publisher-Quartz

